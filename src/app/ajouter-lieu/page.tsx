import PlacesAutocomplete from '@/components/maps/PlacesAutocomplete';

const page = () => {
    return (
        <section className='my-32 text-black'>
            <PlacesAutocomplete />
        </section>
    );
};

export default page;
