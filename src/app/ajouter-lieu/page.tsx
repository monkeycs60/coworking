import PlacesAutocomplete from '@/components/maps/PlacesAutocomplete';

const page = () => {
    return (
        <section className='my-8'>
            <PlacesAutocomplete />
        </section>
    );
};

export default page;
